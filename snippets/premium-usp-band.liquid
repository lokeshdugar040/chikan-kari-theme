<div class="premium-usp-band" aria-label="Purchase benefits">
  <div class="premium-usp-band__inner">
    <div class="premium-usp-band__item">
      <p class="premium-usp-band__label">Offer</p>
      <h3 class="premium-usp-band__title">Extra ₹200 off on 2+ pieces</h3>
      <p class="premium-usp-band__text">Applied automatically at checkout</p>
    </div>

    <div class="premium-usp-band__item">
      <p class="premium-usp-band__label">Shipping</p>
      <h3 class="premium-usp-band__title">Free shipping on prepaid orders</h3>
      <p class="premium-usp-band__text">Tracked delivery across India</p>
    </div>
  </div>
</div>

<style>
  /* Mobile-first: single column card stack, 2 columns from 480px */
  .premium-usp-band {
    width: 100%;
    margin-top: 0;
  }
  .premium-usp-band__inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: var(--space-3, 0.75rem);
    width: 100%;
    padding: var(--space-3, 0.75rem);
    border: 1px solid rgba(110, 88, 65, 0.12);
    border-radius: clamp(1rem, 0.8rem + 1vw, 1.375rem);
    background: linear-gradient(180deg, #fbf8f4 0%, #f4ebe1 100%);
    box-shadow: 0 10px 24px rgba(92, 67, 45, 0.08);
  }
  .premium-usp-band__item {
    position: relative;
    min-width: 0;
    padding: var(--space-4, 1rem);
    border-radius: clamp(0.75rem, 0.6rem + 0.75vw, 1rem);
    background: rgba(255, 255, 255, 0.38);
    box-shadow: inset 0 0 0 1px rgba(110, 88, 65, 0.08);
    overflow: hidden;
  }
  .premium-usp-band__item::before {
    content: "";
    position: absolute;
    inset: var(--space-2, 0.5rem);
    border-radius: 0.75rem;
    border: 1px dashed rgba(110, 88, 65, 0.30);
    pointer-events: none;
  }
  .premium-usp-band__item::after {
    content: "";
    position: absolute;
    right: -1.5rem;
    bottom: -1.5rem;
    width: 5.625rem;
    height: 5.625rem;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(217,181,142,0.30) 0%, rgba(217,181,142,0.10) 42%, transparent 72%);
    filter: blur(8px);
    opacity: 0.7;
    pointer-events: none;
  }
  .premium-usp-band__label {
    margin: 0 0 var(--space-1, 0.25rem);
    color: rgba(110, 88, 65, 0.76);
    font-family: var(--font-body--family, "Noto Sans", sans-serif);
    font-size: var(--text-2xs, 0.6875rem);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }
  .premium-usp-band__title {
    margin: 0;
    color: rgb(110 88 65);
    font-family: var(--font-heading--family, "Martel", serif);
    font-size: var(--text-base, 1rem);
    font-weight: 600;
    line-height: 1.22;
    letter-spacing: -0.01em;
    text-wrap: balance;
  }
  .premium-usp-band__text {
    margin: var(--space-1, 0.25rem) 0 0;
    color: rgba(0, 0, 0, 0.60);
    font-family: var(--font-body--family, "Noto Sans", sans-serif);
    font-size: var(--text-xs, 0.75rem);
    font-weight: 400;
    line-height: 1.45;
  }
  @media screen and (min-width: 480px) {
    .premium-usp-band__inner {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  @media screen and (min-width: 750px) {
    .premium-usp-band__item {
      padding: var(--space-5, 1.25rem) var(--space-5, 1.25rem) var(--space-4, 1rem);
    }
    .premium-usp-band__title {
      font-size: var(--text-lg, 1.125rem);
    }
    .premium-usp-band__text {
      font-size: var(--text-sm, 0.875rem);
    }
  }
</style>